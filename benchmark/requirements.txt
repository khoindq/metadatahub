# Core dependencies for RAG benchmark suite

# Embedding model (used by all systems)
sentence-transformers>=2.2.0

# MetadataHub dependencies
# (Already included in main requirements.txt)
faiss-cpu>=1.7.4

# LlamaIndex
llama-index>=0.10.0
llama-index-embeddings-huggingface>=0.1.0

# LangChain + FAISS
langchain>=0.1.0
langchain-community>=0.0.10
langchain-huggingface>=0.0.1

# Optional: For analysis and visualization
matplotlib>=3.5.0
pandas>=1.4.0

# Development
pytest>=7.0.0
